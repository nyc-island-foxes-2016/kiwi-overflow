<h2> QUESTIONS </h2>

<div id="question-conatiner">
  <ul>
    <% @questions.each do |question| %>
      <li> <%= link_to question.title, question_path(question) %> </li>
    <% end %>
  </ul>
</div>

<% if logged_in? %>
  <%= link_to "Post a New Question", new_question_path %>
<% end %>


